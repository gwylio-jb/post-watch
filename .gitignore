# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Tauri / Rust build artefacts (huge, regenerated every build)
src-tauri/target/
src-tauri/gen/

# Tauri auto-updater signing keys — NEVER commit these.
# The private key lives only on your machine and in GitHub Actions secrets.
*.key
*.pem
~/.tauri/

# Sprint 10 redesign handoff (reference only, not source)
_handoff/
handoff.zip
